"""Synthetic benchmark for the lash extraction pipeline.

This package only *measures* the production code in `backend/`; it never changes
its behaviour. See `evaluation/README.md` for what the numbers mean and what this
benchmark cannot tell us.
"""
